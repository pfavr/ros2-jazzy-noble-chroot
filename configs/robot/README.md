# Robot configs

Version robot-specific launch parameters, device mappings, and deployment
configuration here. Runtime containers should receive mutable data such as bags
through mounted host directories, not through the container writable layer.
